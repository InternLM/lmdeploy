# HF model id: deepseek-ai/DeepSeek-V3

h:
- model_type: chat
  engine_config:
    tp: 16
  backends:
  - name: pytorch
    communicators:
    - nccl
  test_coverage:
  - func
- model_type: chat
  engine_config:
    tp: 16
    extra:
      speculative-algorithm: deepseek_mtp
      speculative-num-draft-tokens: 3
      max-batch-size: 128
  backends:
  - name: pytorch
    communicators:
    - nccl
  test_coverage:
  - func
  - mtp_evaluate
