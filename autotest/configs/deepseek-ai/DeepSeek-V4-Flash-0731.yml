# HF model id: deepseek-ai/DeepSeek-V4-Flash-0731

h:
- model_type: chat
  engine_config:
    tp: 4
  backends:
  - name: pytorch
    communicators:
    - nccl
  test_coverage:
  - evaluate
  - func
  interface:
    pytorch:
    - suites:
      - base
      - logprob
      - toolcall
      - hard_schema
      extra:
        logprobs-mode: raw_logprobs
        enable-return-routed-experts: true
        tool-call-parser: deepseek-v4
        reasoning-parser: deepseek-v4
        enable_thinking: true
  gen_config:
    temperature: 1.0
    top-p: 1.0
