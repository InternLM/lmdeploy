# HF model id: test/test_cp2tp8
# Normalized by autotest/tools/normalize_model_configs.py

test:
- model_type: chat
  engine_config:
    tp: 8
    cp: 2
    extra:
      session-len: 65536
  backends:
  - name: turbomind
    communicators:
    - nccl
  - name: pytorch
    communicators:
    - nccl
  test_coverage:
  - benchmark
  - evaluate
  - func
  - longtext
  - longtext_benchmark
  - prefix_cache
  - quantization
  quantization:
    turbomind:
    - gptq
    - kvint8
    pytorch:
    - kvint8
  gen_config:
    temperature: 0.6
