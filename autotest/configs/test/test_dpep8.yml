# HF model id: test/test_dpep8
# Normalized by autotest/tools/normalize_model_configs.py

test:
- model_type: chat
  engine_config:
    dp: 8
    ep: 8
  backends:
  - name: pytorch
    communicators:
    - nccl
  test_coverage:
  - func
  quantization:
    pytorch:
    - kvint4
    - kvint8
