# HF model id: test/test_tp1_pytorch

test:
- model_type:
  - chat
  - base
  engine_config:
    tp: 1
    extra:
      session-len: 65536
  backends:
  - name: pytorch
    communicators:
    - nccl
  test_coverage:
  - benchmark
  - evaluate
  - func
  - longtext
  - longtext_benchmark
  - prefix_cache
  quantization:
    pytorch:
    - kvint4
    - kvint8
  gen_config:
    temperature: 0.6
