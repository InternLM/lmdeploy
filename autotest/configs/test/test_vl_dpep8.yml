# HF model id: test/test_vl_dpep8
# Normalized by autotest/tools/normalize_model_configs.py

test:
- model_type:
  - chat
  - vl
  engine_config:
    dp: 8
    ep: 8
    extra:
      session-len: 65536
  backends:
  - name: pytorch
    communicators:
    - nccl
  test_coverage:
  - benchmark
  - evaluate
  - func
  - longtext
  - longtext_benchmark
  - prefix_cache
  quantization:
    pytorch:
    - kvint4
  gen_config:
    temperature: 0.6
